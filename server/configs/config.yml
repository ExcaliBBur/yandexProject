port: "8080"
clean_workers_delay_seconds: 120

db:
  host:     "localhost"
  # host:     "db"
  port:     "5432"
  username: "postgres"
  dbname:   "postgres"
  sslmode:  "disable"
  migration_url: "file://migrations"

message-broker:
  url: "amqp://guest:guest@localhost:5672/"
  # url: "amqp://guest:guest@broker:5672/"

